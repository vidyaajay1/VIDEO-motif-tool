export interface DiscoveredMotif {
  id: string;
  pwm: number[][];
  name: string;
  color: string;
}

export interface UserMotif {
  id: string;
  matrix: number[][];
  name: string;
  color: string;
}
